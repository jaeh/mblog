header
  a(href= '/link/' + link.date)= link.title

if link.uploader
  div.uploader= link.uploader
if link.desc
  div.desc= link.desc
if link.category
  div.category
    span.title category: 
    span.category= link.category
